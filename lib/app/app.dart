import "package:flutter/material.dart";

class CmsApp extends StatelessWidget {
  const CmsApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(child: Text("CMS Mobile bootstrap")),
      ),
    );
  }
}
